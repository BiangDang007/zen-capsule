{
  "name": "zen-capsule-extension",
  "version": "0.2.0",
  "private": true,
  "description": "Zen Capsule Chrome Extension — 數位結界",
  "dependencies": {
    "@zen-capsule/shared": "*"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.300",
    "esbuild": "^0.24.0",
    "typescript": "^5.7.0"
  },
  "scripts": {
    "build": "node esbuild.config.mjs",
    "dev": "node esbuild.config.mjs --watch",
    "typecheck": "tsc --noEmit"
  }
}
