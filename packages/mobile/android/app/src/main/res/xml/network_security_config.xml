<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Config for Zen Capsule.
  - Production: HTTPS only (cleartext blocked)
  - Debug: Allow cleartext to 10.0.2.2 (Android emulator → host localhost)
-->
<network-security-config>
    <!-- Default: block cleartext (HTTP) traffic -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug override: allow localhost via emulator -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>

    <!-- Allow cleartext ONLY to emulator localhost (for dev) -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
