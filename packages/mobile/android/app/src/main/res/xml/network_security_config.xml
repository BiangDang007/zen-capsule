<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Config for Zen Capsule.
  - Production: HTTPS only (cleartext blocked)
  - Debug: Allow cleartext to 10.0.2.2 and localhost (emulator → host)
-->
<network-security-config>
    <!-- Default: block cleartext (HTTP) traffic -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow cleartext to emulator localhost for Metro + backend dev server -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>

    <!-- Debug only: trust user-installed certs (e.g. Charles proxy) -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
