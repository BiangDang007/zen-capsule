<?xml version="1.0" encoding="utf-8"?>
<!--
  Network Security Config for Zen Capsule.
  - Production: HTTPS only (cleartext blocked)
  - Debug: Allow cleartext to 10.0.2.2 (Android emulator → host localhost)
-->
<network-security-config>
    <!-- Default: block cleartext (HTTP) traffic -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug only: allow emulator localhost + user-installed certs -->
    <debug-overrides cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
