{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm install && npm -w packages/shared run build && npx -w packages/backend prisma generate && npm -w packages/backend run build"
  },
  "deploy": {
    "startCommand": "cd packages/backend && npx prisma migrate deploy && node dist/index.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
