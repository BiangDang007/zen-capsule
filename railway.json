{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "npm install && npm -w packages/shared run build && npx -w packages/backend prisma generate && npx -w packages/backend prisma migrate deploy && npm -w packages/backend run build"
  },
  "deploy": {
    "startCommand": "node packages/backend/dist/index.js",
    "healthcheckPath": "/health",
    "healthcheckTimeout": 30,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
